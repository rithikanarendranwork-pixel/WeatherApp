# WeatherApp
Weather app developed in Dart and used Flutter for UI
The architecture of the app has 3 layers:

1. Presentation layer that has Flutter UI and Riverpod for state management
2. Domain layer that has abstract classes and entities
3. Data layer that has API calls and creation of Json Objects 
